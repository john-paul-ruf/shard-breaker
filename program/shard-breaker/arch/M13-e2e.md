# M13 — Browser Acceptance Tests

## Boundary

- **Path:** `./tests/e2e/`
- **Session-owned pathspec:** `./tests/e2e/**/*`
- **Purpose:** Verify complete player-visible contracts across real browser
  navigation, pointer input, refresh recovery, persistence, responsive layouts,
  reduced motion, and static-site base paths.

## Public Test Surface

- Feature-scoped `*.spec.ts` files
- Small typed fixtures/helpers under `./tests/e2e/`
- Deterministic test hooks limited to seed/profile setup and diagnostics; no hook
  may bypass the same domain transition rules used by production UI

## Dependency and Implementation Rules

- Tests consume the public app surface from M01–M10 and do not modify source
  modules at runtime.
- Prefer accessible role/name locators and observable committed state. Avoid CSS
  selectors tied only to presentation internals.
- Use deterministic seeds and isolated browser contexts. Clear or seed IndexedDB
  explicitly per test; never depend on test execution order.
- Cover Chromium for the normal gate and the configured Firefox/WebKit/mobile
  projects at release milestones.
- Use the orchestration-assigned port. Trace/screenshot/video output belongs in
  ignored test output paths, never in the session lease unless a deliberate
  checked-in fixture is required.
- Do not create a verification-only implementation session. Add acceptance tests
  to the feature session whose disjoint test file set it owns, or pair them with
  that feature's final integration session.

## Required Flow Families

- Start/resume/abandon guard and safe refresh
- Route choice, mandatory boss floor, shop, and recovery idempotency
- Exactly-three reward choice and refresh-safe one-time commit
- Pointer aim/explicit launch/paddle/loss/clear combat behavior
- Boss telegraph/counterplay/reward transition
- Terminal finalization, record/Shards/relic, export/import/reset
- Desktop, tablet, and portrait-phone ordering plus reduced motion
- GitHub Pages base-path asset and reload behavior

## Change History

| Date | Change |
|------|--------|
| 2026-08-29 | Added cross-browser run-lifecycle acceptance and bounded IndexedDB inspection helpers. |
| 2026-08-29 | Added a precise acceptance-test ownership boundary for Forge session planning. |
| 2026-09-14 | route-drafting SESSION-02: Added route drafting e2e proofs (materialize/select/commit + reload persistence). |

<!-- route-drafting SESSION-02 -->
## Route drafting e2e (route-drafting SESSION-02)

- `run-lifecycle.spec.ts` — added `test.describe("route drafting")` block with 3
  tests: (1) start run → route map visible → 4 cards → reload → same cards
  (IndexedDB offers match), (2) click Battle card → aria-checked → reload → same
  selection, (3) click "Enter selected room" → IndexedDB `phase: "room"`,
  `routeState: null`, `roomState.roomType: "battle"`, `roomState.status: "ready"`
  → reload → still in room phase. Existing e2e tests updated for route-map screen
  (heading "Pick the next pressure point." replaces "Checkpoint restored" for new
  runs; auto-materialize populates offers at revision 1).
- `indexedDb.ts` — `StoredLivingRunRecord` extended with `roomState` fields
  (roomType, status, threatProfile presence, eventKey).
- E2e executed and verified (12 passed: 9 existing + 3 route drafting) on
  `PLAYWRIGHT_PORT=8080`. Not unverified — localhost binding succeeded.

<!-- SESSION-07 -->
## Run-lifecycle acceptance surface

- `./tests/e2e/indexedDb.ts` exports `deleteShardbreakDatabase(page)` and
  `readShardbreakState(page)` plus narrow stored profile/run/state interfaces.
  The helpers close their own connection, bound blocked/version-change/read
  diagnostics, and only delete for isolation or read singleton records; they
  never insert or mutate a run.
- `./tests/e2e/run-lifecycle.spec.ts` isolates every case in a fresh browser
  context and covers default bootstrap, exact class rules, committed start,
  reload/resume without a write, the three-way no-overwrite guard, explicit
  replacement, rapid activation, keyboard/focus/live feedback, 320px and
  portrait-phone geometry, and reduced motion.
- Every case rejects browser console/page errors and non-local HTTP requests.
  The complete matrix contains 36 cases: nine flows across four configured
  projects.
