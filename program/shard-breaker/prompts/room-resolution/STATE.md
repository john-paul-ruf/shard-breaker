# State Tracker — Shard Breaker / room-resolution

## Program / Feature / Intent / Sessions

- **Program:** Shard Breaker (`shard-breaker`)
- **Feature:** room-resolution
- **Intent:** Build the room screen, utility room resolution (shop/recovery), reward draft generation and selection, and depth advancement so a player can commit to a room, complete it, draft a reward, and advance to the next floor — all durable across reload.
- **Sessions:** 3

**Run history (reconciled 2026-09-22):** first execution of this plan
(`f79cd79`–`1fcb139`, run sha256:4079d811…) completed SESSION-01 and was
blocked by a runtime-infrastructure seam at the SESSION-02 dispatch (spawn
validator refusals on `.program/` scratch files — not a product or planning
failure). The run folder was deleted from the worktree during a cleanup;
session prompts were restored from `5790e19`, the executed STATE.md from
`1fcb139`. This STATE.md is the re-published current record with three
planning corrections folded into S02/S03 (prompts carry revision marks): the
empty-route-depth relaxation was removed (S02 now keeps the invariant and
materializes the next route atomically), reward selection gained approved
replacement semantics instead of soft-lock rejections, and the shop e2e
journey was corrected to the real player-visible contract (currency has no
producer until combat). S01 is marked done from the executed record; its
producer outputs are committed and re-verified at the current HEAD
(`npm run test:unit` 260/13, `npm run verify` exit 0, lint 1 pre-existing
RouteMapScreen react-refresh warning).

## Session Status

| # | Session | Modules | Owns | Status | Checkpoint | Completed | Notes |
|---|---------|---------|------|--------|------------|-----------|-------|
| 01 | Reward content and reward draft generator | M02, M03 | `src/domain/content/skills.ts`, `src/domain/content/equipment.ts`, `src/domain/content/enhancements.ts`, `src/domain/content/catalog.ts`, `src/domain/content/catalog.test.ts`, `src/domain/random/generators.ts`, `src/domain/random/generators.test.ts` | done | 2 | 2026-09-15 | CAP-01 producer ready: 8 skills, 8 equipment items, 14 enhancements (unique effectKeys), 6 total catalog lookups, and `generateRewardDraft(catalog, context)` (per-card streams `<roomEventKey>:reward:card:<index>`). `npm run verify` exit 0; 260 tests / 13 files (was 244). CA-01 producer side landed; durability + browser proofs remain S02-CP1 / S03-CP3. |
| 02 | Room resolution domain transitions and store wiring (revision 2) | M05, M01, M07 | `src/domain/run/commands.ts`, `src/domain/run/model.ts`, `src/domain/run/reducer.ts`, `src/domain/run/room.test.ts`, `src/app/commands.ts`, `src/app/appStore.ts`, `src/app/appStore.test.ts`, `src/persistence/validation.ts` | done | 2 | 2026-09-22 | CAP-02/03/04/05 producers landed: four reducer transitions + four store handlers, ten rejection codes, CA-01 durability mapping verified. `npm run verify` exit 0; 297 tests / 14 files (was 260/13). Lease revision 2; one byte-minimal retrospective lease amendment accepted (see Handoff Notes). Browser proofs remain S03-CP3. |
| 03 | Room screen, reward screen, and browser journey (revision 2) | M08, M09, M01, M10, M13 | `src/ui/screens/RoomScreen.tsx`, `src/ui/screens/RoomScreen.test.tsx`, `src/ui/screens/RewardsScreen.tsx`, `src/ui/screens/RewardsScreen.test.tsx`, `src/ui/components/RewardCard.tsx`, `src/ui/components/lifecycleComponents.test.tsx`, `src/app/navigation.ts`, `src/app/App.tsx`, `src/app/App.test.tsx`, `src/styles/global.css`, `src/styles/responsive.css`, `tests/e2e/run-lifecycle.spec.ts`, `tests/e2e/indexedDb.ts` | done | 3 | 2026-09-22 | CAP-06/07 landed; browser proofs for CAP-02..05 executed by Orchestrator at receive: chromium e2e 15/15 (port 8082) incl. 3 new room-resolution journeys + reload determinism. `npm run verify` exit 0; 329 tests / 16 files. Replacement disclosure computed from offered draft + build per the recorded S02 nulling. |

## Wave Plan

| Wave | Sessions | Why concurrent |
|------|----------|----------------|
| 1 | 01 | S02 depends on S01's `generateRewardDraft` and new content types. (Done in the first execution.) |
| 2 | 02 | S02 consumes S01's committed generator + content. |
| 3 | 03 | S03 consumes S02's committed reducer/store contracts. |

Linear plan — no concurrent pairs; every wave has one member because each
session consumes the previous session's committed public API.

## Dependency Graph

```
S01 (done) ──► S02 ──► S03
```

## Architecture Reference (feature-specific only; full config in PROGRAM-CONFIG)

The functional core / imperative shell is built. `AppStore` serializes commands, injects `commitId`/`now`, invokes the pure `runReducer`, and delegates atomic writes to `RunLifecycleRepository.saveCheckpoint`. The pure generators (`generateRouteOptions`, `generateRoomCandidate`, `generateShopInventory`, `generateThreatProfile`, `generateRewardDraft`) in M03 are committed, tested, and read-only. The run lifecycle handles `StartRun`, `AbandonRun`, `MaterializeRoute`, `SelectRouteOffer`, `CommitRoute`. After `CommitRoute`, the living run is in `phase: "room"` with a populated `roomState` (status: "ready", combatCheckpoint: null). Navigation returns `home/checkpoint` for room-phase runs — there is no room screen.

This feature adds: room resolution commands (`BuyShopItem`, `CommitRecovery`, `ResolveRoom`, `SelectReward`) with reward-replacement semantics, store handlers, a `RoomScreen` (showing room state and room-type-specific interactions), a `RewardsScreen` (3-card draft with replacement disclosure), a `RewardCard` component, navigation for room/reward phases, styles, and browser e2e proofs. Combat rooms (battle/elite/boss) show a "combat engine coming soon" placeholder and cannot be resolved — the combat engine (M04/M06) is the next feature.

## Scope Summary (modules affected, indexed by ID)

| ID | Module | Affected | Reason |
|----|--------|----------|--------|
| M02 | Authored content catalog | Done (S01) | `skills.ts` (8), `equipment.ts` (8), `enhancements.ts` (14); catalog lookups for skills/equipment/enhancements. |
| M03 | Seeded generation | Done (S01) | `generateRewardDraft` in `generators.ts`. |
| M05 | Run state machine | Modified (S02) | `BuyShopItem`/`CommitRecovery`/`ResolveRoom`/`SelectReward` commands + reducer transitions; `RewardState` replacement fields. |
| M01 | Application shell | Modified (S02+S03) | New app commands + store handlers (S02); navigation + App routing for room/reward (S03). |
| M07 | Persistence | Modified (S02, bounded) | Two nullable `rewardStateSchema` fields (replacement record). Empty-route rules untouched. |
| M08 | Screens | Modified (S03) | New `RoomScreen`, `RewardsScreen`. |
| M09 | Components | Modified (S03) | New `RewardCard`. |
| M10 | Styles | Modified (S03) | Room and reward screen CSS. |
| M13 | E2e | Modified (S03) | Room resolution + reward draft browser journey. |

No Author artifact (`specs/`, `mocks/`) is modified. Migration 001 is untouched.

## Design Decisions (choice + rationale)

1. **Reward content was created in S01, not by a separate Author session.** The skills, equipment, and enhancements are authored data definitions (immutable catalog content in M02). No Author handoff exists for them — the arch files describe the shapes but no session created the files. Creating them in a Coder session is the conservative path since they are data, not design decisions. The content counts (6-8 skills, 6-8 equipment, 10-15 enhancements) match the requirements constraints. *(Executed 2026-09-15.)*

2. **Combat rooms cannot be resolved in this feature.** The combat domain (M04) and game bridge (M06) do not exist — `src/domain/combat/` and `src/game/` are absent. Building the full fixed-step physics simulation, Canvas renderer, pointer input, collision detection, and boss phases is a separate feature. The RoomScreen shows a "combat engine coming soon" state for battle/elite/boss rooms, and the resolve action is disabled. Utility rooms (shop/recovery) ARE fully resolvable, proving the complete room → reward → next-route journey.

3. **`ResolveRoom` generates the reward draft and transitions to the reward phase.** The room is marked resolved (status: "resolved", resolutionCommitId set), the reward draft is generated from `(seed, contentVersion, depth, roomEventKey)`, and the phase transitions to "reward". The draft is persisted in `rewardState` before the reward screen displays, so a reload returns the same draft.

4. **`SelectReward` generates the reward application, depth advance, AND next-route materialization in one transition.** This avoids an invalid intermediate state (empty route at depth > 1, which the committed persistence validation rejects) and is the change that made the original prompt's validation relaxation unnecessary. The next floor's route offers are generated and persisted atomically with the reward application. The existing `MaterializeRoute` command remains for the initial depth-1 auto-materialize and is not called by `SelectReward` — `SelectReward` calls `generateRouteOptions` directly and maps the results, mirroring the existing `materializeRoute` transition.

5. **Empty-route-depth validation is NOT relaxed (revision-2 correction).** The original S02 prompt ordered a relaxation of `invalid-empty-route-depth` in `src/persistence/validation.ts` — a file outside S02's lease, the seam that stalled the first execution. Because `SelectReward` materializes the next route atomically (Decision 4), no intermediate empty-route-at-depth->-1 state is ever produced or persisted, so the invariant stays true and the edit is unnecessary. The rule and its test row ("empty route after depth one", `validation.test.ts:176`) are preserved untouched. If a future feature legitimately needs that flow, it plans the rule change with the file in its own lease.

6. **Reward application replaces on a full build side (revision-2 correction).** The original prompt rejected a skill card at 3 active skills or an equipment card at 4 passive items, which soft-locks the run (the reward event is already committed and persisted, so no card could ever be applied and depth could never advance). The approved design already prescribes replacement: `mocks/rewards.html` shows "Trade-off replaces Prism Burst · swap shown before commit" and FR-9 requires the card to say what will be displaced before confirmation. `SelectReward` therefore displaces the earliest-acquired item on the full side (lowest index — the append order `SelectReward` itself maintains) and records `displacedRewardId`/`displacedSlot` on the durable `RewardState`; S03's reward screen discloses "REPLACES <name>" before confirmation using the same rule. No new product decision — this is the approved mock/requirements behavior; the rejection design was the deviation.

7. **Recovery is a separate commit from room resolution.** `CommitRecovery` applies the restore amount (clamped to `integrityMax`) and marks `recovery.committed = true`. `ResolveRoom` advances to the reward phase regardless of whether recovery was committed — the player can decline recovery and still advance.

8. **Shop purchases are individual durable actions.** `BuyShopItem` deducts `runCurrency` by the item's price and adds the itemId to `purchasedItemIds`. Repeated purchases of the same item are rejected (`shop-item-already-purchased`). Insufficient currency is rejected (`insufficient-currency`). The room is resolved separately by `ResolveRoom`. The browser journey in S03 proves the visible contract (inventory, prices, unaffordable-disabled-with-reason); purchase success is proven at the reducer/store level (S02-CP2) because runs start at `runCurrency: 0` and no committed producer grants run currency until the combat feature lands.

9. **Room status transitions: "ready" → "in_progress" → "resolved".** `BuyShopItem` and `CommitRecovery` set status to "in_progress" (the player has interacted). `ResolveRoom` sets status to "resolved". This matches the model's status enum and the validation rule that resolved status requires a resolutionCommitId.

10. **Enhancement effects are tracked but not applied in gameplay.** The enhancement IDs are stored in `RewardCardSnapshot.enhancementIds` and `rolledParams`, but their gameplay effects (damage modifiers, charge bonuses, etc.) are deferred to the combat engine feature. The reward application only adds the base skill/equipment to the build.

## Verification Baseline

| Gate | Command | Scope | Evidence | Status |
|------|---------|-------|----------|--------|
| Lint | `npm run lint` | ESLint + Stylelint | Re-executed at replan (2026-09-22, tree at `19e0292`): exit 0, 1 pre-existing react-refresh warning (RouteMapScreen.tsx:55, `createRouteMapScreenModel` export alongside component). Pre-existing; unchanged by this feature until S03 touches the file. | verified |
| Types | `npm run typecheck` | `tsc -b --pretty false` | Re-executed at replan: exit 0. | verified |
| Unit/component | `npm run test:unit` | Discovers `src/**/*.test.ts(x)` | Re-executed by Orchestrator at S03 receive (2026-09-22, after `ee63581`): 329 passed across 16 files (297/14 + RewardCard/lifecycleComponents 38, RoomScreen 12, RewardsScreen 8, App integration +3). | verified |
| Production build | `npm run build` | Typecheck + Vite output in `dist/` | Re-executed by Orchestrator at S03 receive (2026-09-22, after `ee63581`): 155 modules, `dist/assets/index-DFcVKeMJ.js` 403.46 kB, CSS `index-BGZsesB6.css` 28.66 kB. Rollup comment-position warnings from `node_modules/zod/v4/core/{regexes,util}.js` are upstream noise, not product defects. | verified |
| Standard local gate | `npm run verify` | Lint + types + unit + build | Re-executed by Orchestrator at S03 receive (2026-09-22, after `ee63581`): exit 0 — lint 0 errors (1 pre-existing RouteMapScreen react-refresh warning), typecheck clean, 329/16, build 155 modules, `dist/assets/index-DFcVKeMJ.js` 403.46 kB, CSS `index-BGZsesB6.css` 28.66 kB. | verified |
| Chromium journey | `PLAYWRIGHT_PORT=<assigned> npm run test:e2e -- --project=chromium` | `tests/e2e/run-lifecycle.spec.ts` | Re-executed by Orchestrator at S03 receive (2026-09-22, after `ee63581`, port 8082): **15 passed** (12 inherited + 3 new room-resolution journeys). Port-dependent flake observed and excluded by re-run on a fresh port: two inherited lifecycle tests flaked when reusing 8081 seconds after the prior Vite was killed; deterministic green on a clean port (S03's own run passed 15/15 on 8081 mid-session). Recorded flake class: use a fresh port per e2e invocation. Playwright 1.62.1 + chromium-1234 confirmed installed. | verified |

**Known hazards:** (1) The pre-existing RouteMapScreen react-refresh warning —
S03 must not silently "fix" it by moving `createRouteMapScreenModel` (it is
public API consumed by `App.tsx` and its tests); leave it and record if the
count changes. (2) `webServer.reuseExistingServer` is disabled when
`PLAYWRIGHT_PORT` is set — each e2e run starts its own strict-port Vite;
Orchestrator must assign a free port per session slot. (3) The executed run's
`.program/` scratch (decisions/blockers/dispatch) no longer exists; this file
is the authoritative record.

**Build freshness:** `npm run build` at replan produces `dist/assets/index-Bi_jLCZT.js` (376.67 kB). E2e builds via `npm run dev` on the assigned port — dev server serves the current source revision.

## Capability Readiness

| ID | Approved behavior / entry point | Required facts + producer owners | CA IDs / prerequisites | Integration owner / checkpoint | Status | Proof / checked sources | Open gaps + correction owners |
|----|--------------------------------|---------------------------------|------------------------|--------------------------------|--------|-------------------------|-------------------------------|
| CAP-01 | Reward content and draft generator: `generateRewardDraft(catalog, context)` produces exactly 3 unique seeded cards with known content IDs (skills/equipment), enhancement IDs, and material costs. Entry: `generateRewardDraft` in `generators.ts`. | `SkillDefinition` (8), `EquipmentDefinition` (8), `EnhancementDefinition` (14) — produced by S01. `ContentCatalog` lookups — extended by S01. `RewardGenerationContext` — built from living run by S02. | CA-01 | S01-CP1 (content + catalog), S01-CP2 (generator). | verified (producer) | Sources: S01 committed generators.ts; `generators.test.ts` 21 tests pass incl. CA-01 boundary assertions (3 unique cards, catalog-valid base IDs, compatible enhancements, reproducible, varies with seed/depth/eventKey, build-slot aware, minDepth gates, frozen outputs). Orchestrator re-executed `npm run test:unit` (260/13) and `npm run verify` (exit 0) at receive; re-verified at replan and again at S02 receive (297/14, verify exit 0). | Durable mapping landed and verified (S02-CP1, `ea37720`); browser never-reroll proof = S03-CP3. Not user-verified end-to-end yet. |
| CAP-02 | Shop purchase: `BuyShopItem` deducts `runCurrency`, adds itemId to `purchasedItemIds`, persists. Reload preserves purchase. Entry: `room/buy-shop-item` app command → `BuyShopItem` run command → `runReducer` → `saveCheckpoint`. | `RoomState.shop` (inventory, purchasedItemIds) — produced by `CommitRoute` (committed, route-drafting). `runCurrency` — from living run. `itemId` — from shop inventory. | CA-02 | S02-CP1 (domain), S02-CP2 (store), S03-CP3 (browser visible contract). | ready (producer) | Unit/store proofs verified at S02-CP1/CP2 (`ea37720`, `2e8632f`): deduction by stored price, re-purchase/insufficient/unknown-item rejections, room → in_progress, revision bumps; store proof used seeded `runCurrency: 999` fixture. Orchestrator re-executed 297/14 + `npm run verify` exit 0. | **Boundary:** no committed producer grants `runCurrency` (combat feature owns it) — browser journey asserts the unaffordable-disabled contract. Remaining edge (S03 must not resolve in e2e): full side + every draft card already held = all selections rejected; escape today is AbandonRun — product question recorded for Planner. |
| CAP-03 | Recovery commit: `CommitRecovery` applies `restoreAmount` to `integrityCurrent` (clamped to `integrityMax`), marks `recovery.committed`, persists. Entry: `room/commit-recovery` app command → `CommitRecovery` run command → `runReducer` → `saveCheckpoint`. | `RoomState.recovery` (restoreAmount, committed, commitId) — produced by `CommitRoute` (committed, route-drafting). `integrityCurrent`/`integrityMax` — from living run. | CA-03 | S02-CP1 (domain), S02-CP2 (store), S03-CP3 (browser). | ready (producer) | CA-03 unit/store proofs verified at S02-CP1/CP2: clamped restore, `recovery.committed`/`commitId`, re-commit rejection (`room.test.ts`, `appStore.test.ts`). Browser proof planned (S03-CP3). | None. |
| CAP-04 | Room resolution: `ResolveRoom` marks room resolved, generates reward draft, transitions phase → "reward", persists. Reload returns the same draft. Entry: `room/resolve` app command → `ResolveRoom` run command → `runReducer` → `saveCheckpoint`. | `RoomState` (status, eventKey, roomType) — produced by `CommitRoute` (committed). `generateRewardDraft` — produced by S01 (CAP-01, verified producer). | CA-01, CA-04 | S02-CP1 (domain), S02-CP2 (store), S03-CP3 (browser). | ready (producer) | CA-04 unit/store proofs verified at S02-CP1/CP2: phase → reward with 3-card draft, `progress.roomsResolved` bump, combat/already-resolved rejections; `roomState` nulled per phase-state coherence (`validation.ts` `unexpected-room-state`). CA-01 durability mapping verified (`mapRewardCard`/`mapRewardDraft` field-preserving; `parseLivingRunRecord` accepts). Browser proof planned (S03-CP3). | Combat rooms (battle/elite/boss) blocked — no combat engine. |
| CAP-05 | Reward selection and depth advancement: `SelectReward` applies the selected card to the build (replacement on a full side), increments depth, materializes the next route, transitions phase → "route", persists. Reload returns the same advanced state. Entry: `reward/select` app command → `SelectReward` run command → `runReducer` → `saveCheckpoint`. | `RewardState` (cards, selectedCardId) — produced by `ResolveRoom` (CAP-04). `generateRouteOptions` — M03, committed. Build slot limits — validated by `validation.ts`. | CA-01, CA-04, CA-05 | S02-CP1 (domain), S02-CP2 (store), S03-CP3 (browser). | ready (producer) | CA-05 unit/store proofs verified at S02-CP1/CP2: apply + depth/cycle advance + next-route materialization, replace-earliest on a full side (proven on the equipment side), already-selected/unknown-card rejections; applied `RewardState` is transient — transition nulls `rewardState` per phase-state coherence (`validation.ts` `unexpected-reward-state`), displacement disclosed via store signal. Browser proof planned (S03-CP3). | `displacedRewardId`/`displacedSlot` are always null on produced durable states (transient applied record) — S03 computes the pre-confirm replacement preview from the offered draft + build (per SESSION-02.md disclosure contract). All-duplicate-draft dead-end (full side + every card held) is an open product question owned by Planner — no command path advances that draft; escape today is AbandonRun. |
| CAP-06 | Room screen: `RoomScreen` displays the committed room state (objectives, threat, shop/recovery/boss content) with room-type-specific interactions and an "advance" action. Combat rooms show a "coming soon" placeholder. Entry: `deriveScreen` returns `room` for `phase === "room"`. | `RoomState` — from living run. `RoomDefinition` display fields — from catalog. Shop inventory — from `roomState.shop`. Recovery offer — from `roomState.recovery`. | CA-02, CA-03, CA-04 | S03-CP1 (RoomScreen), S03-CP2 (RewardsScreen + wiring), S03-CP3 (browser). | verified | CAP-06 verified: component tests 12 (shop inventory + disabled-with-reason buys, recovery offer + commit, combat placeholder + disabled resolve, busy disabling, catalog-resolved room name); `deriveScreen` returns room for room-phase (App.test.tsx); browser journeys passed at S03-CP3 (Orchestrator re-run 15/15, port 8082; S03's own run 15/15 port 8081). View-model deviations recorded in Handoff Notes (additive objectiveNames/precomputed affordability; threatProfile dropped — nothing renders it). | Combat rooms show placeholder. |
| CAP-07 | Reward screen: `RewardsScreen` displays exactly 3 `RewardCard`s with fully revealed base reward, enhancement, cost, and trade-off, plus the before-commit replacement disclosure on full build sides. Selection + confirm dispatches `reward/select`. Entry: `deriveScreen` returns `reward` for `phase === "reward"`. | `RewardState.cards` — produced by `ResolveRoom` (CAP-04). `SkillDefinition`/`EquipmentDefinition`/`EnhancementDefinition` display fields — from catalog (S01, committed). Build counts — from living run. | CA-01, CA-05 | S03-CP2 (RewardsScreen + wiring), S03-CP3 (browser). | verified | CAP-07 verified: exactly 3 catalog-resolved cards, radiogroup selection (staged locally; only Confirm dispatches `reward/select` — committed producer applies immediately), confirm disabled without selection, replacement disclosure computed from offered draft + build via replace-earliest (FR-9); browser leg in the reward-phase journey (Orchestrator re-run 15/15). Deviation: prompt prescribed onSelect-dispatch + durable `selectedCardId` in the view model — rejected per CP0 protocol (producer applies immediately on dispatch; durable post-apply concept). | None. |

**First narrow journey:** Start → route map → select Recovery → commit → room screen shows recovery offer → commit recovery → resolve room → reward screen shows 3 cards → select a card → confirm → route map for depth 2 with materialized offers → reload returns the same advanced state. (The shop journey is the same shape with the visible-contract buy state; the purchase leg is reducer/store-proven until combat grants currency.)

## Contract Agreements

| ID | Required meaning / authority | Producer → boundary → consumer | Mapping / constraints | Correction + proof owners / checkpoints | Agreement | Producer | Proof / evidence / checked sources |
|----|------------------------------|--------------------------------|-----------------------|----------------------------------------|-----------|----------|------------------------------------|
| CA-01 | A reward draft persists exactly 3 unique seeded cards for `(runSeed, contentVersion, depth, roomEventKey)`. Cards never reroll on reload. Each card has a known base reward ID, reward type, enhancement IDs, rolled params, and material cost. | S01 `generateRewardDraft` → `RewardState.cards: [RewardCardSnapshot, RewardCardSnapshot, RewardCardSnapshot]` → S02 `runReducer` (`ResolveRoom`) → S03 `RewardsScreen`. | `GeneratedRewardCard` → `RewardCardSnapshot`: keep `cardId`, `baseRewardId`, `rewardType`, `enhancementIds`, `rolledParams`, `materialCost`, `tradeoffId`. `rewardType ∈ {"skill", "equipment"}` for initial build. Each `baseRewardId` must exist in catalog. Each `enhancementId` must exist and be compatible with the reward type. Exactly 3 unique `cardId`s. Draft is persisted before display. | S01-CP2 (generator, done), S02-CP1 (reducer mapping + persistence, done — `ea37720`), S03-CP3 (browser reload proof). | agreed | ready (producer; durability verified) | Generator producer verified at S01-CP2 (21 generator tests, all boundary assertions). Durability proof verified at S02-CP1: `mapRewardCard`/`mapRewardDraft` preserve all seven fields, frozen; draft persisted in `rewardState` before display; every resulting living run accepted by `parseLivingRunRecord`; Orchestrator re-executed 297/14 at S02 receive and 329/16 + `npm run verify` exit 0 at S03 receive. Browser never-reroll proof verified at S03-CP3: reload determinism asserted in the recovery journey (same draft after reload; reward-phase journey cross-checks the applied `build` entry against the offered draft's `baseRewardId`); chromium e2e 15/15 re-executed by Orchestrator (port 8082). |
| CA-02 | A shop purchase deducts `runCurrency` by the item's price and adds `itemId` to `purchasedItemIds`. The purchase is durable. Re-purchase rejected. Insufficient currency rejected. | S02 `runReducer` (`BuyShopItem`) → `RoomState.shop.purchasedItemIds` + `LivingRun.runCurrency` → S02 `saveCheckpoint` → S03 `RoomScreen`. | `itemId` must be in `shop.inventory`. `price` from `shop.inventory.find(i => i.itemId === itemId).price`. `runCurrency -= price` (must stay >= 0). `purchasedItemIds` must not already contain `itemId`. Room status → "in_progress". Revision bumps. | S02-CP1 (domain, done), S02-CP2 (store, done), S03-CP3 (browser visible contract). | agreed | ready (producer) | Unit/store proofs verified at S02-CP1/CP2 (`ea37720`, `2e8632f`): deduction by stored price, purchasedItemIds growth, room → in_progress, revision bumps; re-purchase/insufficient-currency/unknown-item rejections; store publishes only after `saveCheckpoint`. Browser proof verified at S03-CP3: shop journey asserts 2 items named/priced, both Buy controls disabled with a visible reason at `runCurrency: 0`, unaffordable contract preserved across depth advance + reload. Currency-producer boundary recorded in CAP-02 (combat feature). |
| CA-03 | A recovery commit applies `restoreAmount` to `integrityCurrent` (clamped to `integrityMax`), marks `recovery.committed = true` with `commitId`. Durable. Re-commit rejected. | S02 `runReducer` (`CommitRecovery`) → `RoomState.recovery.committed/commitId` + `LivingRun.integrityCurrent` → S02 `saveCheckpoint` → S03 `RoomScreen`. | `restoreAmount` from `roomState.recovery.restoreAmount` (authored as 1). `integrityCurrent = min(integrityCurrent + restoreAmount, integrityMax)`. `recovery.committed` must be false. `recovery.commitId` set to `commitId`. Room status → "in_progress". Revision bumps. | S02-CP1 (domain, done), S02-CP2 (store, done), S03-CP3 (browser). | agreed | ready (producer) | Unit/store proofs verified at S02-CP1/CP2: clamped restore (`min(current + restoreAmount, max)`), `recovery.committed`/`commitId`, room → in_progress, re-commit rejection. Browser proof verified at S03-CP3 (recovery journey incl. the 3→3 clamp assertion + reload; Orchestrator re-run 15/15). |
| CA-04 | Room resolution marks `roomState.status = "resolved"`, sets `resolutionCommitId`, generates the reward draft, transitions `phase: "room" → "reward"`, populates `rewardState`, nulls `roomState`. Durable. Re-resolve rejected. | S02 `runReducer` (`ResolveRoom`) → `RoomState.status/resolutionCommitId` + `RewardState` + `LivingRun.phase` → S02 `saveCheckpoint` → S03 `RoomScreen`/`RewardsScreen`. | Room must be in "ready" or "in_progress" status. Combat rooms (battle/elite/boss) rejected with `combat-not-implemented`. `resolutionCommitId = commitId`. `rewardState` from `generateRewardDraft(catalog, context)`. `phase: "reward"`, `roomState: null`. `progress.roomsResolved += 1`. Revision bumps. | S02-CP1 (domain, done), S02-CP2 (store, done), S03-CP3 (browser). | agreed | ready (producer) | Unit/store proofs verified at S02-CP1/CP2: phase room → reward, 3-card `rewardState` (status offered), `progress.roomsResolved` bump, `roomState` nulled in the same transition (phase-state coherence per `validation.ts`), combat/already-resolved rejections. Browser proof verified at S03-CP3 (resolve → reward phase with 3 offered cards, roomState null in IndexedDB; Orchestrator re-run 15/15). |
| CA-05 | Reward selection applies the selected card's base reward to the build (adding, or replacing the earliest-acquired same-side item on a full side — `activeSkillIds[0]`/`passiveEquipmentIds[0]` — with the displaced ID recorded durably), increments depth, materializes the next route, transitions `phase: "reward" → "route"`, nulls `rewardState`, populates `routeState`. Durable. Re-select rejected. | S02 `runReducer` (`SelectReward`) → `BuildSnapshot` + `RewardState.displacedRewardId/displacedSlot` + `LivingRun.depth/cycle/routeState` → S02 `saveCheckpoint` → S03 `RewardsScreen`/`RouteMapScreen`. | `selectedCardId` must be in `rewardState.cards`. `rewardState.status` must be "offered" (not already selected/applied). If skill and side not full: append to `activeSkillIds` (≤ 3). If skill and side full: replace `activeSkillIds[0]`, record `displacedRewardId`/`displacedSlot = "active"`. Same rule for equipment vs `passiveEquipmentIds` (limit 4, `"passive"`). `depth += 1`, `cycle = cycleForDepth(depth)`. `routeState` from `generateRouteOptions(catalog, context)` mapped to `RouteOfferSnapshot[]`. `phase: "route"`, `rewardState: null`, `rewardState.status = "applied"`, `selectionCommitId = commitId`. Revision bumps. Replacement semantics per approved `mocks/rewards.html` ("replaces X · swap shown before commit") and FR-9. | S02-CP1 (domain + schema, done), S02-CP2 (store, done), S03-CP2 (disclosure display, done), S03-CP3 (browser, done). | agreed (revised 2026-09-22) | ready (producer; display + browser verified) | Unit/store proofs verified at S02-CP1/CP2: apply + depth/cycle advance + next-route materialization (route context mirrors `materializeRoute` incl. `routeEventKey`), replace-earliest on a full side (equipment side proven; skill-side slots-aware draft returns all-equipment by design), already-selected/unknown-card rejections, invalid-state on depth overflow. **Correction per phase-state coherence:** the applied `RewardState` is transient — `selectReward` nulls `rewardState` in the same transition (`validation.ts` `unexpected-reward-state` rejects retaining it), so `displacedRewardId`/`displacedSlot` are always null on produced durable states; S03's screen computes the pre-confirm preview from the offered draft + build (landed and unit-tested at CP2; browser reward-phase journey at CP3 proves confirm → depth 2 + reload determinism, Orchestrator re-run 15/15). **Revision record:** the original agreement (rejections `active-skills-full`/`passive-equipment-full`) is superseded — replacement per approved mock/FR-9 is the agreement of record. |

## Current Blockers

All three sessions are done; CAP-01..07 are verified against current sources (see Capability Readiness for the CAP-02 currency boundary and the CAP-05 all-duplicate-draft product question). No blockers to this run. Combat rooms (battle/elite/boss) remain approved-deferred to the combat-engine feature. The shop-currency browser proof is a recorded boundary owned by the combat feature (purchase mechanics store-proven; browser asserts the unaffordable contract). One product question remains recorded for Planner (CAP-05 open gaps): the all-duplicate-draft dead-end (full build side + every draft card already held) has no advancing command path today — escape is AbandonRun. Recorded follow-up for Archivist/Planner: a `listRouteSupport()` catalog facade lookup would keep objective-name resolution behind the facade (App.tsx currently reads `ROUTE_SUPPORT_DEFINITIONS` directly; already-declared M06→M01 edge, not a contract break). E2e flake class recorded: a fresh port per e2e invocation avoids the save-signal/strict-order flake seen when reusing a port seconds after a prior Vite was killed. No schema- or design-author re-entry required.

## Handoff Notes (Orchestrator writes here after each session — from Coder's Handoff section, verbatim)

<!-- room-resolution SESSION-01 — received 2026-09-15, done at checkpoint 2 (carried from executed run 4079d811) -->
- **session:** 01
- **status:** done
- **checkpoint:** 2
- **notes:** CAP-01 producer contribution landed at lease rev 1: `SKILL_DEFINITIONS` (8), `EQUIPMENT_DEFINITIONS` (8), `ENHANCEMENT_DEFINITIONS` (14, unique effectKeys), catalog lookups `listSkills/getSkill`, `listEquipment/getEquipment`, `listEnhancements/getEnhancement` (total, frozen), and `generateRewardDraft(catalog, context)` in `generators.ts`. CA-01 producer ready: exact 7-field `GeneratedRewardCard`→`RewardCardSnapshot` mapping implemented per agreement; `rewardType` stays the narrow `"skill" | "equipment"` (durable 5-value mapping is S02's). CP0 recheck performed and clean: `src/domain/run/model.ts` (`EffectParam {key; value: string|number|boolean}`, `RewardCardSnapshot`, `RewardState.cards` 3-tuple) and `src/persistence/validation.ts` (`rewardCardSchema`, `rewardStateSchema`) match the authoritative shapes — zero persistence edits made (outside lease; schemas already accept the output, and `effectParamsSchema`'s unique-key rule is satisfied because catalog construction rejects duplicate enhancement effectKeys). Existing generator/lookup patterns confirmed before writing. Changed mapping from prompt: (1) card stream is `<roomEventKey>:reward:card:<index>` (prompt showed `card:${i}` under the parent key; parent key alone would share a cursor with nothing else but keeps the room-level key unnamed and unused — index-scoped sub-keys used instead, preserving isolation); (2) prompt's "5 enhancements with minDepth 3" authored as 4 at depth-3 + 1 at depth-2 (`enhancement-focused`, keeping ≥3 per gate as the prompt requires); (3) rolled param values are seeded ints 1–4 (prompt's "e.g." made concrete). Remaining proof owners: S02-CP1 (reducer mapping + persistence of the draft), S03-CP3 (browser reload = never-reroll proof). This session proves the generator producer only; CAP-01/CA-01 are not user-verified end-to-end yet.
- **delivered:** 8 skill defs + 8 equipment defs + 14 enhancement defs as frozen catalog data with construction-time validation (ID format/dupes, `maxCharges ≥ 1`, `minDepth` safe ≥1, unique enhancement effectKeys); `ContentCatalog` extended with 6 total lookups; `RewardGenerationContext`/`GeneratedRewardCard`/`GeneratedRewardDraft` types and deterministic `generateRewardDraft` — per-card streams `(seed, contentVersion, "<roomEventKey>:reward:card:<index>")`, ID-sorted pools before seeded pick, 0–2 compatible `minDepth`-gated enhancements per card, per-enhancement rolled `EffectParam` (1–4), material cost 0–9, `tradeoffId: null`, build-slot-aware reward kinds, all outputs frozen, `TypeError`/`RangeError` rejections.
- **verification:** Baseline at f79cd79 reproduced first (typecheck 0; 244 passed/13 files). CP1 gate `npm run test:unit -- src/domain/content/catalog.test.ts src/domain/content/rooms.test.ts` → 26 passed (2 files, counts read; 9 new catalog tests). CP2 gate `npm run test:unit -- src/domain/random/generators.test.ts` → 21 passed (7 new reward tests). Full `npm run test:unit` → 260 passed / 13 files. `npm run verify` (CP2 requirement) exit 0: lint 0 errors (2 warnings — both the pre-existing RouteMapScreen.tsx react-refresh finding; one from a stale archived worktree copy under gitignored `.program/`, per Orchestrator preflight, not my output), typecheck clean, build 152 modules, `dist/assets/index-Bi_jLCZT.js` 376.67 kB (was 369.95 kB), CSS byte-identical `index-DRRq2BV-.css`. CA-01 boundary assertions all landed in `generators.test.ts`: exactly 3 unique cardIds (+ exact cardId format), all baseRewardIds resolve in catalog by type, all enhancementIds exist + compatible + unique per card with rolledParams pairing 1:1, reproducible same context (also isolated from 200 unrelated draws), varies with seed/depth/eventKey, build-slot awareness (full skill side → all equipment, full equipment side → all skills), minDepth respected with positive controls (minDepth-2/3 enhancements appear once their gate is reached, never before), frozen outputs (draft, cards, nested arrays) + mutation rejection. No e2e in this session per envelope.
- **surprises:** (1) One test-failure iteration: my initial depth-gate test wrongly asserted depth-1 seen-enhancements ⊆ depth-3 seen — depth participates in the stream tuple so the drafts are independent draws; corrected to bound + positive-control assertions (implementation unchanged, no contract impact). (2) `lint` reports the RouteMapScreen warning twice because eslint also scans a stale archived worktree copy under gitignored `.program/worktrees/` — matches Orchestrator's preflight note; outside my lease, untouched. (3) Prompt's enhancement count "10–15 (optionally 5 named at minDepth 3)" was internally inconsistent; authored 14 with 4 at depth 3 + 1 at depth 2, preserving the required ≥3 per gate. (4) One-off tsc strictness fixes during development (readonly-tuple cast via `unknown` in test; comparator for `EnhancementDefinition`); all resolved before checkpoint commits. Lease revision used: 1. Two checkpoint commits: `6b55327` (5 paths), `ad5b08b` (2 paths) — both verified inside the write set via `git show --name-only`.
- **followUp:** S02 must map `GeneratedRewardCard` → `RewardCardSnapshot` (narrow → wide `rewardType` union) and persist `RewardState` before display (CA-01 durability proof at S02-CP1); `RewardGenerationContext` needs `roomType` + both slot counts from the living run + `roomState`; note the both-slots-full edge returns all-equipment cards by design (reducer authoritative behavior stays required — STATE.md decision 6, now replacement semantics per revision 2). S03-CP3 owns the browser reload proof. Archivist: reconcile arch M03 "Depends on M02 only" with the authorized type-only `EffectParam` import (recorded in arch M03 fragment already); no STATE.md edits were made by me (Orchestrator-owned).
- **needsOwnerCorrection:** —
- **needsDesignSource:** —
- **blockedReason:** —
- **filesTouched:** `src/domain/content/skills.ts`, `src/domain/content/equipment.ts`, `src/domain/content/enhancements.ts`, `src/domain/content/catalog.ts`, `src/domain/content/catalog.test.ts`, `src/domain/random/generators.ts`, `src/domain/random/generators.test.ts`
<!-- room-resolution SESSION-02 — received 2026-09-22, done at checkpoint 2 -->
- **session:** 02
- **status:** done
- **checkpoint:** 2
- **notes:** CAP-02/03/04/05 producers landed (CAP-02 boundary held: purchase proven at store level with seeded `runCurrency: 999` fixture; no committed producer grants currency — browser contract stays S03). CA-01 mapping proof landed (`GeneratedRewardDraft` → `RewardState` via `mapRewardCard`/`mapRewardDraft`, field-preserving, frozen; accepted by `parseLivingRunRecord`). CA-02/03/04/05 producers ready; unit proofs verified. CA-05 displacement record: `RewardState.displacedRewardId/displacedSlot` exist in type + zod schema, but the committed reducer always nulls `rewardState` on apply (see surprises) — the fields are currently always null on produced durable states; S03's pre-confirmation disclosure must compute the replacement preview from the offered draft + build (as SESSION-02.md's own disclosure-contract note already specifies). Lease revision 2 used. Checkpoints: `ea37720` (7 paths), `2e8632f` (3 paths) — both verified inside the write set via `git show --name-only`.
- **delivered:** `BuyShopItem`, `CommitRecovery`, `ResolveRoom`, `SelectReward` pure reducer transitions with `save-checkpoint` persistence, rejection codes, and `requireOpenRoomPhase` helper; generator-to-snapshot mapping (`mapRewardCard`, `mapRewardDraft`, reused `mapRouteOffer`); `RewardState` two-field extension in model + zod schema; four app commands, four store handlers with replacement-disclosure save signal; `room.test.ts` (30 tests) + 7 new `appStore.test.ts` integration tests.
- **verification:** CP1 gate `npx vitest run` on the four named suites → 4 files selected (verified both directions), 125 passed, exit 0; `npm run typecheck` exit 0 at commit time. CP2 gate `npm run verify` exit 0 — lint 0 errors (1 pre-existing RouteMapScreen react-refresh warning, untouched per baseline), typecheck clean, `npm run test:unit` **297 passed / 14 files** (was 260/13), build 152 modules, `dist/assets/index-DiBfe3ta.js` 390.23 kB. Boundary assertions exercised: currency deduction + re-purchase + insufficient-currency rejections (CA-02), clamped integrity restore + re-commit rejection (CA-03), reward phase with 3 unique cards + combat rejection + already-resolved rejection (CA-04), depth advance + next-route materialization + full-side replacement + already-selected/unknown-card rejections (CA-05), reload preservation via `parseLivingRunRecord` on every resulting living run, and the untouched `invalid-empty-route-depth` rule still passing ("empty route after depth one", Correction 1). No e2e per envelope.
- **surprises:** Lease revision 2 used. (1) **Two contradictions inside SESSION-02.md's implementation section, both resolved against the authoritative behavior:** (a) step 5's `resolveRoom` update list kept the resolved `roomState` alongside `phase: "reward"` — `src/domain/run/validation.ts` (`unexpected-room-state`) rejects that state, and the prompt's own CP1 spec + CAP-04 observable success require `roomState === null`; the transition nulls it (the resolved status/`resolutionCommitId` therefore exist only transiently inside the reducer; the durable record of resolution is the consumed room + incremented `progress.roomsResolved`). (b) step 6's note said the applied `RewardState` is "kept as the terminal record," then immediately says it "is nulled together with the phase transition" — incoherent; `validation.ts` (`unexpected-reward-state`), CAP-05 and CA-05's observable success (`rewardState === null`) all require nulling, so `selectReward` nulls it and builds the applied record only transiently; displacement is disclosed via the store signal ("Reward selected; replaced <name>. Advancing to Depth N."). (2) **Known edge (STATE.md decision 6) observed at store level:** with a full equipment side, the seeded draft's first card may duplicate a held item; the reducer correctly rejects it (`invalid-active-skills`-class validation), so the store replacement test selects the first non-held card — deterministic for the fixture seed; S03 must handle a fully-duplicate draft (all 3 cards held) as the remaining dead-end case (no command path advances that specific draft; see followUp). (3) The slots-aware generator produced an all-equipment draft for a full skill side (by design), so the store replacement proof runs on the equipment side. (4) My own scratch-file writes were rejected once (outside-repo path) and one misdirected scratch command failed without writing — source tree verified pristine after both; several defective large-file drafts I authored mid-session were caught by self-review before running and fully rewritten (final committed code was verified hunk-by-hunk against originals). (5) `npm run test:unit -- <patterns>` confirmed non-filtering (documented); all gates run via `npx vitest run <paths>` with file counts read in both directions.
- **followUp:** **S03:** `deriveScreen` currently has no `room`/`reward` screens — room/reward-phase runs fall back to CHECKPOINT (home) as before; wire `RoomScreen`/`RewardsScreen` + navigation + `RoomTypeForRejection`-aware disabled states (combat resolve shows the bounded message). **S03 replacement disclosure:** compute the preview from the offered draft + current build (replace-earliest rule); `RewardState.displacedRewardId/displacedSlot` are always null on durable states this session produces — if a future surface wants the applied record retained, that is a contract change needing its own planning (the transient record is built in `selectReward` and discarded). **S03 must also plan the all-duplicate-draft edge** (full side + every card already held → every selection rejected as invalid state): today the player can only abandon the run from there; if the approved design wants a different escape (e.g. allow replacing with an identical item), it is a product decision for Planner. **Combat feature** owns the `runCurrency` producer (shop purchases store-proven only, per CAP-02 boundary). Archivist: reconcile arch M03/M01/M07 with the arch fragment; STATE.md was not touched by me (Orchestrator-owned).
- **needsOwnerCorrection:** (see block below)
- **needsDesignSource:** —
- **blockedReason:** —
- **filesTouched:**
  - `src/domain/run/commands.ts`
  - `src/domain/run/model.ts`
  - `src/domain/run/reducer.ts`
  - `src/domain/run/room.test.ts`
  - `src/app/commands.ts`
  - `src/app/appStore.ts`
  - `src/app/appStore.test.ts`
  - `src/persistence/validation.ts`
  - `src/persistence/validation.test.ts` (mechanical seam, see below)

### needsOwnerCorrection (SESSION-02 — accepted by Orchestrator 2026-09-22)

- **kind:** mechanical-owner-seam
- **paths:**
  - `src/persistence/validation.test.ts`
- **whyNeeded:** Correction 2 made the two new nullable fields **required** on `RewardState` (durable strict-schema record). The read-only fixture `makeRewardRun()` in `src/persistence/validation.test.ts` constructs a full `RewardState` literal, so without the fix the change breaks `tsc` (TS2739 at line 93, `tsconfig.app.json` includes `src`) and the runtime CA-01 persistence-acceptance test "requires exactly three unique reward cards with legal rolled params" (empirically proven at base `0fd63a3` before fixing). Both failures are the contract change itself landing on its only literal constructor; test files compile under the same project, so producer and consumer proof had to land in the same checkpoint.
- **editMade (committed in `ea37720`):** two lines added to `makeRewardRun()`: `displacedRewardId: null,` / `displacedSlot: null,` after `selectionCommitId: null,`. No assertion or behavior change; the "empty route after depth one" row and every other test row pass unchanged.
- **affectedChecks:** `npm run typecheck`; `npx vitest run src/persistence/validation.test.ts` (26 tests pass).
- **lastAcceptedCheckpoint:** CP1 (`ea37720`); carried through CP2's full `npm run verify`.
- **requestedLeaseRevision:** retrospective authorization only — the edit is already committed and byte-minimal; no further action needed if accepted.
- **unblocks:** SESSION-02 (this session), SESSION-03 (reads the schema's consumer tests).

<!-- room-resolution SESSION-03 — received 2026-09-22, done at checkpoint 3 (UI discipline) -->
- **session:** 03
- **status:** done
- **checkpoint:** 3
- **delegatedTo:** ui-coder (session routed to UI discipline per envelope; executed by Coder under UI-CODER.md — risk lives in the render)
- **lease revision:** 2 (revision-2 prompt of 2026-09-22 used throughout; no amendments needed)
- **notes:** CAP-06 producer contribution landed (RoomScreen: shop inventory with prices + disabled-with-reason unaffordable buys, recovery offer + commit, combat/boss placeholder with resolve disabled, dispatching `room/buy-shop-item`/`room/commit-recovery`/`room/resolve`; `deriveScreen` now returns `{id:"room"}` for `phase === "room"`). CAP-07 producer contribution landed (RewardsScreen: exactly-3-card radiogroup, confirm bar, `reward/select` on Confirm only; `deriveScreen` returns `{id:"reward"}` for `phase === "reward"`; RewardCard with catalog-resolved name/description/enhancements/cost/trade-off row and the FR-9 replacement disclosure computed per Correction 2 from offered draft + build via the replace-earliest rule — `displacedRewardId`/`displacedSlot` never read from durable state, per the recorded S02 nulling). CA-01 display-mapping proof landed: card names/descriptions/enhancement labels resolve through `catalog.getSkill`/`getEquipment`/`getEnhancement` by durable IDs (`RewardsScreen.test.tsx`, `lifecycleComponents.test.tsx`). CA-02 visible contract landed (shop buy disabled + "Not enough room shards" on 0-currency runs); CA-03/CA-04/CA-05 unit proofs inherited green and browser legs exercised below. These are producer contributions plus browser journeys — CAP-02..07 are not user-verified beyond the Playwright chromium run recorded here. All-duplicate-draft edge untouched (e2e journeys run from empty builds; cannot hit it) — remains Planner's recorded product question.
- **delivered:** `RewardCard` (accessible radio card, glyph by rewardType, enhancement block, material cost, trade-off row, "REPLACES <name> · swap shown before commit" disclosure, 44px targets, roving-focus radio keyboard); `RoomScreen` (status bar, room header with depth/cycle/integrity/room-shards stats, objectives, shop/recovery/combat-placeholder content, SaveSignal, advance action); `RewardsScreen` (draft header with capacity + no-reroll note, 3-card radiogroup, confirm bar with selection + replacement readout); `deriveScreen` room/reward branches; `App.tsx` `createRoomModel`/`createRewardsModel` + render branches; room/reward styles in `global.css` (tokens only, state never color-only) and responsive reflow in `responsive.css` (≤980 horizontal-scroll card row, ≤720 stacks, reduced-motion); e2e `room resolution` block (recovery full journey with clamp assertion + reload determinism/never-reroll, shop visible-contract journey, IndexedDB assertions) and `StoredLivingRunRecord` extensions (`rewardState`, `runCurrency`, `build`).
- **verification:** Baseline held. CP1: `npx vitest run src/ui/screens/RoomScreen.test.tsx src/ui/components/lifecycleComponents.test.tsx` → 2 files selected (read in both directions; suites fully covered), 50 passed. CP2: `npm run verify` exit 0 — lint 0 errors (1 pre-existing RouteMapScreen react-refresh warning, untouched per baseline), typecheck clean, `npm run test:unit` 329 passed / 16 files (was 297/14), build 155 modules, `dist/assets/index-BxHOnxic.js` 403.46 kB, CSS `index-DRRq2BV-.css` 18.74 kB at CP2 / 28.66 kB at CP3 (my additions). CP3: `npm run verify` exit 0 again (329/16, build `index-DFcVKeMJ.js` + `index-BGZsesB6.css` 28.66 kB); `PLAYWRIGHT_PORT=8081 npm run test:e2e -- --project=chromium` → **15 passed** (12 inherited + 3 new room-resolution journeys: recovery commit clamps 3→3, resolve → `phase:"reward"` with 3 offered cards in IndexedDB, `roomState:null`, confirm → `phase:"route"`, depth 2, `routeState.offers` length 4, all state identical after reload; shop journey proves 2 items named/priced, both Buy controls disabled with visible `.shop-item__reason` ×2, `runCurrency: 0` untouched, depth advance + reload; third test cross-checks applied `build` entry against the offered draft's `baseRewardId`). CA-01 mapping exercised by unit + e2e (`build` contains the confirmed draft card's base ID after reload). Stylelint passes on both stylesheets (BEM disable-header comment preserved). Pixel/render inspection (UI Rule 4): geometry probe on the live dev server (my build, port 8081) across 1280/900/390/320 — zero horizontal overflow in every class, zero sub-44px hit targets, computed-style probe confirms shop grid 2-col→1-col, reward cards 3-col→1-col, selected card carries `--glow-cyan` + cyan check fill in all classes. NOTE: screenshot PNGs were captured and stored under gitignored `test-results/` but my read tool cannot decode PNG binaries, so visual inspection was performed via computed-style/geometry probes of the live DOM, not eyeballed images — recorded here honestly; screenshots remain on disk for a human to review. (Also: first e2e run failed 2 tests on strict-mode locator ambiguities in my own assertions — fixed, not product defects.)
- **surprises:** (1) **Prompt contradiction resolved per checkpoint-0 protocol:** CP2 step 2 prescribed `onSelect → dispatch({type:"reward/select"})`, but the committed producer applies immediately on `reward/select` (reducer nulls `rewardState`, rejects non-`"offered"`), which would have eliminated the confirm step CAP-07/CA-05/e2e require. Radiogroup selection stages component-locally; only Confirm dispatches. `RewardsScreen` view model likewise omits the prescribed `selectedCardId` field (it is a durable post-apply concept; staging is local state). (2) Prescribed view-model shapes lacked data their own specs require — additive: `objectiveNames` (catalog has no route-support lookup, so names come from `ROUTE_SUPPORT_DEFINITIONS`), `catalog` on RewardsScreen (for displaced-name resolution), `activeSkillIds`/`passiveEquipmentIds` via `build` (prompt prescribed scalar counts, insufficient for the earliest-item preview); RoomScreen adds `objectiveNames` + precomputed shop item display/affordability, drops `threatProfile` (prompt listed it; nothing renders it — combat UI is a placeholder). (3) Prompt's "Integrity `3 → 3` clamp at full" journey is ambiguous about *what* it proves — I assert `integrityCurrent === 3` after commit, which proves no over-restore but not the clamp arithmetic itself (that remains S02's reducer proof); a damaged-start e2e would need a wallet/damage producer that does not exist. (4) Shop journey fixtures did not need a non-duplicating seed override — journeys start from empty builds, so the all-duplicate-draft dead-end is unreachable in e2e. (5) Playwright 1.62.1 + chromium-1234 worked as Orchestrator verified; port 8081 bound, verified free before and released after (`webServer.reuseExistingServer` disabled with PLAYWRIGHT_PORT set — each run started its own strict-port Vite). (6) `git show` settle-checks on all three commits verified every path inside the write set.
- **followUp:** **Combat feature** owns the `runCurrency` producer (shop purchases remain store-proven; browser journey asserts the unaffordable contract — record the boundary in the final report). **Planner** owns the all-duplicate-draft dead-end (STATE.md CAP-05 open gaps). **Next feature (combat engine M04/M06):** RoomScreen's combat branch is a placeholder — replace it with the arena; the `Advance to reward draft` resolve for combat rooms stays disabled until then. **Archivist:** reconcile M08/M09/M10/M13 module tables and the M06→M01 `ROUTE_SUPPORT_DEFINITIONS` read with `.program/signal/SESSION-03.arch.md`; consider a `listRouteSupport()` catalog lookup so objective-name resolution doesn't reach past the facade (small follow-up, not a contract break — the import is the already-declared M06→M01 edge). **STATE.md was not touched by me** (Orchestrator-owned); STATE.md should record: S03 done at checkpoint 3; CAP-02/03/04/05/06/07 → verified per the chromium e2e above with the CAP-02 currency boundary noted; CA-01/02/03/04/05 proofs verified (unit + e2e, CA-02 browser = visible contract only); actual counts: 329 unit / 16 files, 15/15 chromium e2e.
- **needsOwnerCorrection:** —
- **needsDesignSource:** —
- **blockedReason:** —
- **filesTouched:**
  - `src/ui/components/RewardCard.tsx`
  - `src/ui/components/lifecycleComponents.test.tsx`
  - `src/ui/screens/RoomScreen.tsx`
  - `src/ui/screens/RoomScreen.test.tsx`
  - `src/ui/screens/RewardsScreen.tsx`
  - `src/ui/screens/RewardsScreen.test.tsx`
  - `src/app/navigation.ts`
  - `src/app/App.tsx`
  - `src/app/App.test.tsx`
  - `src/styles/global.css`
  - `src/styles/responsive.css`
  - `tests/e2e/run-lifecycle.spec.ts`
  - `tests/e2e/indexedDb.ts`
